import {createApp, h} from 'vue'import OCR from './OCR.vue'import {NConfigProvider, NMessageProvider} from "naive-ui";// 确保 naive-ui 全局变量存在// @ts-ignoreif (typeof window.naive === 'undefined') {    console.error('naive-ui is not available in global scope');    throw new Error('naive-ui dependency not found');}// 注册为普通组件而不是 Web Componentconst elementId = 'plugin-container';const container = document.getElementById(elementId);if (container) {    // 从主应用获取主题配置    const themeOverrides = window.themeColors ? {        common: {            primaryColor: window.themeColors.value.universal.primary,            primaryColorHover: window.themeColors.value.universal.primary,            primaryColorPressed: window.themeColors.value.universal.primary,            borderRadius: '5px',        },        Input: {            color: window.themeColors.value.universal.background,            colorFocus: window.themeColors.value.universal.background,            colorHover: window.themeColors.value.universal.background,            colorDisabled: window.themeColors.value.universal.background,            border: `1px solid ${window.themeColors.value.universal.border}`,            borderHover: window.themeColors.value.universal.border,            borderFocus: window.themeColors.value.universal.border,            borderDisabled: window.themeColors.value.universal.border,            textColor: window.themeColors.value.universal.text,            textColorDisabled: window.themeColors.value.universal.textHint,            placeholderColor: window.themeColors.value.universal.textHint,            placeholderColorDisabled: window.themeColors.value.universal.textHint,            borderRadius: '5px',        },        Message: {            color: window.themeColors.value.universal.background,            colorInfo: window.themeColors.value.universal.background,            colorSuccess: window.themeColors.value.universal.background,            colorError: window.themeColors.value.universal.background,            colorWarning: window.themeColors.value.universal.background,            colorLoading: window.themeColors.value.universal.background,            textColor: window.themeColors.value.universal.text,            textColorInfo: window.themeColors.value.universal.text,            textColorSuccess: window.themeColors.value.universal.text,            textColorError: window.themeColors.value.universal.text,            textColorWarning: window.themeColors.value.universal.text,            textColorLoading: window.themeColors.value.universal.text,            margin: '40px 0',        },        Divider: {            color: window.themeColors.value.universal.border,            textColor: window.themeColors.value.universal.text,        },    } : {};    const app = createApp({        render: () => h(NConfigProvider, { themeOverrides },            h(NMessageProvider, null,                h(OCR)            )        )    });    app.mount(container);    const styleId = 'ocr-plugin-styles';    if (!document.getElementById(styleId)) {        const style = document.createElement('style');        style.id = styleId;        style.textContent = `            #plugin-container .n-divider:not(.n-divider--vertical) {                margin-top: 0 !important;                margin-bottom: 2px !important;            }            #plugin-container .n-input__suffix {                align-items: flex-end !important;            }        `;        document.head.appendChild(style);    }} else {    console.error(`Container element with id "${elementId}" not found`);}